#ifndef ASCII_ART_H
#define ASCII_ART_H

#include <string>

/**
 * @brief Generates ASCII art representation of the given text.
 * 
 * This function takes a string input and converts it into an ASCII art
 * representation. The specific style and format of the ASCII art will
 * depend on the implementation details of the function.
 * 
 * @param text The input string to be converted into ASCII art.
 * @return A string containing the ASCII art representation of the input text.
 */
std::string generateAsciiArt(const std::string& text);

#endif // ASCII_ART_H